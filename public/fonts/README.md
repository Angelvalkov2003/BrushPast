# Optional DIN font files

Place licensed DIN webfont files here when available:

- `DIN-Regular.woff2`
- `DIN-Medium.woff2`
- `DIN-Bold.woff2`

Until then, the site falls back to Inter (`--font-bp-subtitle`) via `globals.css`.
Stick No Bills and Oswald load from `next/font/google`.
